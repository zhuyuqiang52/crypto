I have summarized the features and created the `factor_pool.py` file. I've also provided a summary of the features in the `feature_summary.md` file.

Now I will explain how to run the `factor_pool.py` script.
To run the script and see the computed factors, you can execute the following command in your terminal:

```bash
python factor_pool.py
```

This will load the data, compute all the implemented factors, and print the first few rows of the resulting dataframe.
